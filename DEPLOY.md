# Render で公開する手順（Starter プラン・月$7・常時稼働）

3人がどこからでも使えて、予約データも消えない公開方法です。
設定ファイル（`render.yaml` / `Dockerfile`）は用意済みなので、以下を順にクリックするだけです。

所要時間：約10分。必要なもの：GitHubアカウント、Renderアカウント、クレジットカード。

---

## 1. Render に登録・ログイン

1. https://render.com を開く
2. **「Get Started」→ GitHub で続行**（Sign in with GitHub）
3. 画面の指示に従い、Render から GitHub への接続を許可する

## 2. このリポジトリを Blueprint で読み込む

1. Render のダッシュボード右上 **「New +」→「Blueprint」** を選ぶ
2. リポジトリ一覧から **`gaogao999/kurumayoyaku`** を選ぶ
   - 出てこない場合は「Configure account」で対象リポジトリへのアクセスを許可
3. **Branch（ブランチ）** で **`claude/hopeful-faraday-62k8iq`** を選ぶ
   - ※ `main` に取り込み済みなら `main` を選んでOK
4. Render が `render.yaml` を自動で読み込み、内容（Web サービス＋1GBディスク）を表示します
5. **「Apply」** をクリック

## 3. プラン（課金）の確認

- `render.yaml` で **Starter（$7/月）** を指定しています。
- 初回に支払い方法（クレジットカード）の登録を求められたら登録してください。
- 永続ディスク（1GB）が含まれます。これにより**再デプロイやスリープ復帰でも予約データは消えません**。

## 4. ビルド完了を待つ

1. デプロイが始まり、数分でビルド → 起動まで進みます
2. ステータスが **「Live」** になれば完了
3. 画面上部に発行された URL（例：`https://kurumayoyaku.onrender.com`）が表示されます
4. その URL を開いて画面が出ればOK（`/healthz` で `{"ok":true}` が返れば正常稼働）

## 5. 3人で使い始める

1. 発行された URL を3人に共有
2. 各自が画面上部の **「わたしは」** で自分を選ぶ（端末に記憶されます）
3. 必要なら **「名前を変更」** で「メンバー1/2/3」を実名に変更（誰か1人がやればOK）
4. あとは各日の **「＋ 追加」** から、翌週以降の予定を各自入力

---

## よくある質問

**Q. URL を知っていれば誰でも見られる？**
A. はい。3人だけに URL を共有する運用が基本です。「合言葉（簡易パスワード）」を付けたい場合は対応できます。

**Q. 月$7 を3人で割りたい**
A. 課金は登録者1人にまとまります。割り勘は3人の間で精算してください。

**Q. もっと安く／無料にしたい**
A. 無料で永続化したい場合は Google スプレッドシート方式や Cloudflare Tunnel も選べます（作り直し・別設定が必要）。

**Q. データのバックアップは？**
A. Render の Disk は永続ですが、念のため必要なら定期バックアップの仕組みも追加できます。

---

## 困ったときのチェック

- **ビルドが失敗する**：Render の「Logs」を確認。`render.yaml` のブランチ指定が正しいか確認。
- **画面は出るが予約が保存されない**：環境変数 `DB_PATH=/data/kurumayoyaku.db` とディスクの mountPath `/data` が一致しているか確認（`render.yaml` 既定でOK）。
- **スリープして初回アクセスが遅い**：Starter プランは基本スリープしませんが、起動直後は少し待ってください。
